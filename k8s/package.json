{
  "name": "k8s",
  "scripts": {
    "format": "prettier --write **/*.{ts,yaml,json}",
    "generate:crds": "./generate-crds.sh"
  },
  "dependencies": {
    "@pulumi/kubernetes": "^3.0.0",
    "@pulumi/kubernetesx": "^0.1.5",
    "@pulumi/pulumi": "^3.0.0",
    "json-schema-library": "^6.0.0",
    "lodash": "^4.17.21",
    "yaml": "^2.1.0"
  },
  "devDependencies": {
    "@types/node": "^14",
    "prettier": "^2.6.2"
  }
}
