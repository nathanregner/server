{
  "name": "k8s",
  "scripts": {
    "format": "prettier --write **/*.{ts,yaml,json}"
  },
  "devDependencies": {
    "@types/node": "^14",
    "prettier": "^2.6.2"
  },
  "dependencies": {
    "@pulumi/kubernetes": "^3.0.0",
    "@pulumi/kubernetesx": "^0.1.5",
    "@pulumi/pulumi": "^3.0.0"
  }
}
